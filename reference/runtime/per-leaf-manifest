# per-leaf-manifest — ALLOW-LIST for verify-merged per-leaf suite filter (issue #194)
#
# Format:
#   ALLOW    <basename>  — leaf-unit-safe; included in per-leaf verify-merged
#   EXCLUDED <basename>  — heavy/timing/process; GHA full suite only (ci.yml:21-33)
#
# Criterion ALLOW (fail-safe): verdict = pure function of merged content only.
#   No launcher loops, no background processes, no timing/poll/sleep asserts,
#   no recursive verify-merged calls.  Any doubt ⇒ EXCLUDED (fail-closed default).
#
# Guard invariant (fail-closed):
#   ALLOW ∪ EXCLUDED == all reference/tests/*.test.sh   (union is complete)
#   ALLOW ∩ EXCLUDED == ∅                               (disjoint)
#   Any unclassified *.test.sh ⇒ guard RED.
#   To add a test to ALLOW you MUST document it here with a criterion justification.
#
# Counts: ALLOW=21, EXCLUDED=54, union=75  (verify with: leaf-verifier.test.sh Test 7)
#
# #206 Part B: runtime-manifest promoted EXCLUDED→ALLOW. Its verdict is a pure
#   function of merged content (sha256 / find / awk / grep — no launcher loop, no
#   background process, no timing/poll/sleep, no recursive verify-merged), so it
#   meets the ALLOW criterion. Promotion is SOUND only because verify-merged
#   regenerates the sha-lock IN-CLONE before the gate suite (#206 Part A): without
#   that, benign sha-drift would false-RED Test 3; with it, Test 3 stays green on
#   drift while Test 1/2/4 still catch structural breaks (missing row / removed
#   runtime file / wrong status). This also retires the #130 flake (transient
#   drift-fail) for the per-leaf gate.
# #206 also adds charter-finale-regen (the A-persistence integration test): heavy
#   git (clone/merge/push to prove persistence on MAIN) ⇒ EXCLUDED. union 45→46.

# ── ALLOW (20 — leaf-unit-safe: verdict = f(merged content), no processes/timing) ──
ALLOW acceptance-block
ALLOW cli-smoke
ALLOW gate-comment-payload
ALLOW launchable
ALLOW manifest-lib
# #238: manifest-stat — pure awk/grep count of TSV content; no processes/timing/launcher.
ALLOW manifest-stat
ALLOW marker-gate
ALLOW prep-params
ALLOW runtime-lint
ALLOW runtime-manifest
ALLOW spawn-unit
ALLOW token-hygiene
# #217 leaf-lint: verdict = pure function of body text (grep/awk parse of
#   Charter:/Depends-on: fields; no launcher loops, no background processes,
#   no timing/poll/sleep). Meets ALLOW criterion.
ALLOW leaf-lint
# #137 role-spawn: spawn-unit stub (no launcher loop, no background processes, no timing);
#   verdict = pure function of merged prompt content + work-tree agent file.
ALLOW role-spawn
# #131 P2/Ф1 manifest pipeline — pure-content tests (no launcher loop/spawn/sleep):
ALLOW board-states
ALLOW composition-parse

# ── EXCLUDED (54 — launcher-self / integration / timing / heavy-git / recursive-verify) ──
EXCLUDED base-freshness
EXCLUDED charter-branch
EXCLUDED charter-finale
EXCLUDED charter-finale-regen
EXCLUDED ci-taxonomy
EXCLUDED deploy-contract
EXCLUDED deploy-step
EXCLUDED deploy-verify
EXCLUDED doctor-drift
EXCLUDED doctor-process
EXCLUDED finale-nonblocking
EXCLUDED gate-charter-tree
EXCLUDED gate-integrator
EXCLUDED gate-layer-a
EXCLUDED gate-layer-b
EXCLUDED gate-repo-dir
EXCLUDED infra-false-green
EXCLUDED integrator
EXCLUDED integrator-atomicity
EXCLUDED integrator-loop
EXCLUDED integrator-scope
EXCLUDED killswitch
EXCLUDED launcher-dryrun
EXCLUDED launcher-gh-scope
EXCLUDED launcher-integration
EXCLUDED launcher-queue
EXCLUDED leaf-verifier
EXCLUDED leaf-verifier-cache
EXCLUDED needs-rework
EXCLUDED plan-spawn
EXCLUDED provision-build
EXCLUDED rework-contract
EXCLUDED run-env-contract
EXCLUDED run-liveness
EXCLUDED ui-api-contract
EXCLUDED ui-loop-mode
EXCLUDED ui-run-contract
# #131 P2/Ф1 manifest pipeline — invoke launcher/prep-spawn (heavy/launcher-context):
EXCLUDED legacy-smoke
EXCLUDED manifest-connect
EXCLUDED techlead-manifest-prompt
EXCLUDED analysis-spawn
# #138 approval-gate: integration-stub that drives the REAL launcher (heavy/launcher-context).
EXCLUDED approval-gate
# #334 convergence: analyst↔reviewer loop integration-stub driving the REAL launcher (heavy).
EXCLUDED convergence
# #382 plan-convergence: tech-lead↔analyst plan-review loop integration-stub driving the REAL
#   launcher (background spawns, multi-tick poll loop) — heavy/launcher-context. EXCLUDED.
EXCLUDED plan-convergence
# #423 queue-plan-convergence: queue-mode variant of the plan-convergence integration-stub
#   (drives the REAL launcher, background spawns, poll loop) — heavy/launcher-context. EXCLUDED.
EXCLUDED queue-plan-convergence
# #139 launchable-composition: pure-predicate unit test (Layer 1: stdin fixture → launchable.sh;
#   Layer 2: board-gh.sh + gh stub synchronous). No launcher loop, no background processes,
#   no timing/poll/sleep. Verdict = f(merged content only). Meets ALLOW criterion.
ALLOW launchable-composition
# #273 ops-roles: pure unit (grep/parse of .claude/agents role files; no processes/timing/launcher).
ALLOW ops-roles
# #356 agentconfig-fswork: pure unit (crewboss-spawn.sh CB_SPAWN_DRYRUN hook + grep; exits before
#   jail/proxy/loop; no background/timing). AgentConfigMap P1 fs.work → /work mount mode.
ALLOW agentconfig-fswork
# #368 gitignore-pyc: pure content check — grep .gitignore + git ls-files; no launcher loop,
#   no background processes, no timing/poll/sleep. Verifies #367 hygiene fix is in place.
ALLOW gitignore-pyc
# #140 manifest-e2e: end-to-end acceptance test driving the REAL launcher through the full
#   manifest pipeline (needs-plan → needs-analysis → team-review → needs-plan → plan-review
#   → approved → role-leaves → integrator-merge → idle). Uses background processes, real
#   git operations, timing/poll loops — heavy/launcher-context. EXCLUDED (fail-closed).
EXCLUDED manifest-e2e
# #255 finale-autorebase: integration-stub driving the real launcher + git bare remote
#   to test auto-rebase of charter behind main in the finale cycle. Heavy: launches the
#   real launcher loop, real git clone/merge/push ops, timing-sensitive. EXCLUDED (fail-closed).
EXCLUDED finale-autorebase
# #262 blast-radius-schedule: integration-stub driving the REAL launcher loop
#   (cmd_run, unscoped) to verify the blast-radius scheduler gate. Launcher loop +
#   background spawns — heavy/launcher-context. EXCLUDED (fail-closed).
EXCLUDED blast-radius-schedule
# #270 role-schema: integration/api test that starts crewboss-api.py daemon, posts
#   roles with model+accesses, verifies round-trip and file frontmatter. Starts a
#   real daemon (background process, timing-sensitive). EXCLUDED (fail-closed).
EXCLUDED role-schema
# #276 git-resolver-wiring: integration-stub driving the REAL launcher loop to verify
#   conflict-resolution cycle (conflict-spawn stub, real board-gh.sh + launcher).
#   Background spawns, launcher loop, timing-sensitive. EXCLUDED (fail-closed).
EXCLUDED git-resolver-wiring
# #449 gc-stale-leaves: integration-stub driving the REAL launcher loop to verify
#   stale-leaf GC (close type:agent leaves when plan-reviewer critiques → needs-plan;
#   numsAfter prefix-isolation #5 vs #50). Launcher loop, background spawns. EXCLUDED.
EXCLUDED gc-stale-leaves
# #391 plan-decomposition: integration-stub driving the REAL launcher loop to verify
#   the leaf-count gate (require_decomp_leaves manifest fixture; stub with/without leaves,
#   multi-tick poll loop) — heavy/launcher-context. EXCLUDED (fail-closed).
EXCLUDED plan-decomposition
# #400 merge-action: starts crewboss-api.py as background process to test
#   merge write-action HTTP scenarios (happy/no-pr/red-gate/merge-fails).
#   Background/timing-sensitive -- same class as ui-loop-mode, ui-run-contract.
#   EXCLUDED (fail-closed).
EXCLUDED merge-action
# #490 per-charter-auto-gate: integration test: real launcher + per-charter label stubs; heavy process -> EXCLUDED
EXCLUDED per-charter-auto-gate
# #533 independent-test-authorship: pure stdin JSON piped to gate binary; no network, no gh-shim,
#   no launcher loops, no background processes, no timing/poll/sleep. Verdict = f(merged content only).
#   Tests Edit/Write/MultiEdit blocking for executor on test-file paths (charter #523 ITA gate).
ALLOW independent-test-authorship
