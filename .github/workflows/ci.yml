# no-op — crewboss runs its own engine-suite locally; GHA billing is dropped.
name: ci
on: [push, pull_request]
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "GHA disabled — crewboss box is the CI engine."
