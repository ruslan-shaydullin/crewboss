# _box-snapshot — временный снапшот боевого рантайма EC2 (~/cbnet)

Доставлен оператором (#61) для чартера #60 (P1.5). Это **сырой снапшот** боевого
рантайма, который сейчас существует ТОЛЬКО на боксе (источник долга «бокс↔репо»).

- `cbnet/` — 67 скриптов рантайма (scp с бокса; БЕЗ run/state/токенов/секретов/.bak).
- Лист **#62** инвентаризирует это → строит runtime-манифест (sha256, canon vs cruft).
- Лист **#65** бэк-портирует КАНОН отсюда в `reference/runtime/` (с тестами).
- После Фазы 0 этот каталог УДАЛЯЕТСЯ (он — строительные леса, не часть продукта).

НЕ source of truth — это копия для бэк-порта. Канон после #65 = `reference/runtime/`.
