# crewboss — UI roadmap (web-дашборд)

> Цель (слова юзера): не инженерный тулз, а **комфортный для всех** продукт — интуитивный,
> широко функциональный, **по-настоящему красивый**, **геймифицированный** (анимации +
> клиентский путь), **предсказуемый** («знаешь, что будет, нажав любую кнопку» + защита от
> ребёнка). Текущий curses-TUI ([proto/r10](proto/r10/)) — заглушка, заменяется.
>
> **Платформа: web (React).** Единственная, что даёт анимации/геймификацию/красоту/адаптив.
> Ложится на уже готовое разделение **Engine↔View** (§9 дизайна): движок (bash на боксе)
> пишет файлы/борд; UI — отдельный клиент, читает через тонкий API. Ядро уже работает
> (3a→R11) — это трек ПОВЕРХ него, не переписывание.
>
> **Правило:** ни один пункт не выкидывается. Порядок = фазы по зависимостям; внутри фазы —
> сверху вниз. Статус: ☐ не начато.

## Фаза 0 — Фундамент (блокирует всё; без него UI некуда подключать)
- ☐ **0.1 demon-API на боксе** — HTTP/WS-демон поверх движка: отдаёт `status.json`/`budget.json`/борд, принимает команды (run/pause/kill/approve/create). Заменяет «UI лезет в файлы по SSH».
- ☐ **0.2 realtime-канал** — WebSocket/SSE: живые обновления без поллинга.
- ☐ **0.3 auth UI↔демон** — токен + TLS.
- ☐ **0.4 стек фронта + сборка + дистрибуция** — как юзер это запускает (часть «из коробки»).
- ☐ **0.5 модель состояния на клиенте** — борд, агенты, бюджет, события.

## Фаза 1 — Каркас и клиентский путь (зависит от Ф0)
- ☐ **1.1 карта экранов**: Дашборд · Доска (charters→leaves) · Карточка задачи · Live-агент(лог) · **Команда/Org** (иерархия ролей, см. [org-model.ru.md](org-model.ru.md): org-chart + drag-drop редактор поверх манифеста) · Бюджет · Boss-чат · Настройки.
- ☐ **1.2 навигация** между экранами.
- ☐ **1.3 онбординг** — первый запуск, подключение токенов (claude OAuth, GH PAT), «создай первую задачу», пустые состояния с подсказками.
- ☐ **1.4 клиентский путь** — сквозной сценарий: завести чартер → увидеть план → одобрить → смотреть работу агентов → ревью PR. Спроектировать каждый шаг.
- ☐ **1.5 состояния экранов** — loading / empty / error / **offline (демон недоступен)** для каждого.

## Фаза 2 — Визуальный язык / дизайн-система (параллельно Ф1)
- ☐ **2.1 основы** — палитра, тёмная/светлая темы, типографика, сетка, отступы.
- ☐ **2.2 компоненты** — карточка задачи, статус-бейдж, прогресс агента, бюджет-гейдж, тулбар, модалки, тосты.
- ☐ **2.3 иконография** + визуальные состояния статусов (open/in-progress/review/blocked/held/done).
- ☐ **2.4 айдентика** — логотип/бренд crewboss.

## Фаза 3 — Функциональность (действия из UI; зависит от Ф0-API + Ф2-компонентов)
- ☐ **3.1 создать/редактировать** задачу и чартер (форма вместо `gh issue create`).
- ☐ **3.2 управление планом** — approve/reject, hold/unhold, kill #N, retry blocked.
- ☐ **3.3 управление лаунчером** — run/pause/kill, настройка cap/бюджета/автономии.
- ☐ **3.4 live-лог агента** — стриминг с редактом секретов.
- ☐ **3.5 drill-down** — задача → её PR, диф, **граф зависимостей**.
- ☐ **3.6 Boss-чат** — интерактивная сессия, attach/detach (отдельный режим).
- ☐ **3.7 история** — прошлые прогоны.

## Фаза 4 — Предсказуемость и защита от ребёнка (поверх Ф3; критично для «комфортно для всех»)
- ☐ **4.1 ясность кнопок** — подпись + иконка + tooltip «что произойдёт».
- ☐ **4.2 превью действия** до выполнения — особенно `run` («запустит N агентов, ~$X»).
- ☐ **4.3 подтверждения** для дорогих/необратимых (run/kill/approve/merge) — диалог с явными последствиями.
- ☐ **4.4 disabled с объяснением** «почему нельзя сейчас».
- ☐ **4.5 undo** где возможно; «ты уверен» только для реально опасного (не задалбывать).
- ☐ **4.6 всегда видно** — уровень автономии + бюджет-кап крупно.

## Фаза 5 — Анимации и переходы (поверх Ф2/Ф3)
- ☐ **5.1 переходы состояний задачи** (open→in-progress→review→done) анимированно.
- ☐ **5.2 карточки** — появление/исчезновение, реордеринг.
- ☐ **5.3 живой агент** — прогресс работающего, «печатает»-индикатор.
- ☐ **5.4 микровзаимодействия** — hover/press/disabled, загрузочные скелетоны.

## Фаза 6 — Геймификация (поверх Ф5)
- ☐ **6.1 прогресс/достижения** — задач закрыто, PR смержено, стрики.
- ☐ **6.2 флот агентов** как персонажи/юниты; статусы = состояния.
- ☐ **6.3 бюджет как ресурс/энергия** — игровая метафора пула.
- ☐ **6.4 празднование успеха** — звук/конфетти на PR открыт / чартер закрыт (выключаемо).
- ☐ **6.5 уровни/прогресс проекта**.

## Фаза 7 — Доступность, комфорт, персонализация (сквозное; не в конце «галочкой»)
- ☐ **7.1 клавиатура + хоткеи**, мышь/тач.
- ☐ **7.2 a11y** — контраст, размеры шрифта, **motion-reduce** (выключить анимации).
- ☐ **7.3 локализация** ru/en (паттерн Lingui уже есть в Quarter).
- ☐ **7.4 адаптив** — десктоп/планшет/телефон.
- ☐ **7.5 настройки** — темы, плотность, вкл/выкл звук/анимации, частота обновления.
- ☐ **7.6 профиль/токены** — ввод и хранение (claude OAuth, GH PAT); конфиг лаунчера через UI, не env.

## Фаза 8 — Обратная связь и качество
- ☐ **8.1 нотификации** — тосты на события (PR открыт, задача blocked, бюджет на исходе).
- ☐ **8.2 realtime-индикатор** «связь с движком жива».
- ☐ **8.3 тесты** — компонентные/визуальные + e2e клиентского пути.
- ☐ **8.4 кроссбраузер/кросс-ОС**.
- ☐ **8.5 перф** — анимации без лагов, realtime не течёт.

## Зависимости (граф, коротко)
```
Ф0 (API+realtime+стек) ─┬─> Ф1 (каркас/путь) ─┬─> Ф3 (функции) ─> Ф4 (предсказуемость)
                        └─> Ф2 (дизайн-система)┘         └─> Ф5 (анимации) ─> Ф6 (геймификация)
Ф7 (a11y/локаль/адаптив) — сквозное, проектируется с Ф1, не откладывать в конец.
Ф8 (качество) — по мере готовности каждого экрана.
```

## Честные оговорки
- Это **отдельный продуктовый трек**, кратно больше ядра по объёму. Ядро (движок) уже работает и UI его не меняет — общается только через контракт.
- Связь с остальным backlog'ом: некоторые ядровые пункты всплывут как требования UI (boss сведён в прототип → нужен для 3.6; merge→done → нужен для полного «ревью PR» в 1.4).
- Геймификация (Ф6) — максимальная свобода, но строится последней: ей нужны живые данные/события из Ф3-5.
