You are the executor for issue #533 in repo stratch1989/crewboss.
Hard rules for THIS run:
- You are ALREADY on branch leaf/533-1782151006, based on the charter integration branch 'charter/523' (NOT main). Sibling leaves of charter #523 may already be merged into 'charter/523'. Commit your work on THIS branch. Do NOT create or switch to any other branch.
- When the work is done and the verification gate is green, push this branch (git push -u origin HEAD) and open ONE pull request: gh pr create --base charter/523 --title '<short>' --body 'Closes #533'. The PR base MUST be 'charter/523', NOT main. Then STOP — do not merge, do not touch other issues.
- This issue is self-contained; everything you need is below.

---- TASK (issue #533) ----
Charter: #523

## Context
Charter #523 goal: separate test authorship from implementation. Tests must be derived from charter/analysis INTENT by a role that does NOT write the implementation code. This leaf runs FIRST; all implementation leaves depend on it. This leaf is assigned to qa-engineer. Derive tests from the charter body and analysis comment on issue #523 ONLY - do NOT read any executor leaf spec.

## Key facts from analysis comment on #523
- crewboss-gate.sh currently only fires for tool_name=Bash (line 26); no handling for Edit/Write/MultiEdit.
- .claude/settings.json has a single PreToolUse entry with matcher Bash (lines 11-22).
- reference/runtime/per-leaf-manifest has ALLOW=20, EXCLUDED=54, union=74.
- leaf-verifier.test.sh Test 7 hard-codes those counts at lines 244, 264, 267, 270.
- All three tools (Edit, Write, MultiEdit) share tool_input.file_path at the top level. tool_input.edits[].file_path is null on real MultiEdit payloads.

## Task

### 1. Write reference/tests/independent-test-authorship.test.sh

**(a) Gate blocks executor from editing test files via Edit, Write, and MultiEdit.**
For each tool name, construct JSON with tool_input.file_path pointing to a protected path (e.g. foo.test.sh, foo.test.ts, src/tests/bar.sh) and role=executor. Pipe to .claude/hooks/crewboss-gate.sh and assert exit 2 (deny). Also assert a legitimate executor path (e.g. src/tests-utils.ts) exits 0 (no false-deny).

**(c) verify-merged presence check.**
Assert reference/tests/independent-test-authorship.test.sh exists on disk.

Assertion (b) gh-pr-ready interlock is intentionally omitted - enforced structurally by launcher Depends-on; no gate extension needed.

### 2. Update reference/runtime/per-leaf-manifest
Add independent-test-authorship as ALLOW (pure stdin JSON against gate binary, no network, no gh-shim). Include criterion justification comment matching existing ALLOW entry format.

### 3. Update reference/tests/leaf-verifier.test.sh Test 7 counts
- Line ~244 label: update to ALLOW=21 EXCLUDED=54
- Line ~264: eq 20 -> eq 21
- Line ~267: eq 54 (unchanged)
- Line ~270: eq 74 -> eq 75
New totals: ALLOW=21, EXCLUDED=54, union=75.

### 4. Post a crewboss-digest comment on this issue
Summarise assertions covered, ALLOW rationale, and updated counts.

## Acceptance (machine)
- test: reference/tests/independent-test-authorship.test.sh
- check: bash reference/tests/independent-test-authorship.test.sh
- check: grep -q independent-test-authorship reference/runtime/per-leaf-manifest
- check: grep -q "eq 21" reference/tests/leaf-verifier.test.sh
- check: grep -q "eq 75" reference/tests/leaf-verifier.test.sh
- check: bash reference/tests/leaf-verifier.test.sh