You are the executor for issue #1161 in repo stratch1989/crewboss.
Hard rules for THIS run:
- You are ALREADY on branch leaf/1161-1782905562, based on the charter integration branch 'charter/506' (NOT main). Sibling leaves of charter #506 may already be merged into 'charter/506'. Commit your work on THIS branch. Do NOT create or switch to any other branch.
- When the work is done and the verification gate is green, push this branch (git push -u origin HEAD) and open ONE pull request: gh pr create --base charter/506 --title '<short>' --body 'Closes #1161'. The PR base MUST be 'charter/506', NOT main. Then STOP — do not merge, do not touch other issues.
- This issue is self-contained; everything you need is below.

---- TASK (issue #1161) ----
Charter: #506
Depends-on: #1160

## Review Findings — impl-lookahead (#1160) — APPROVED

All 5 criteria verified against commit 68522c9 (merged as PR #1164 into charter/506).

### 1. Both guards replaced — PASS
- Loop 1 (plannable_scoped path, lines 1931-1933): replaced `[ -z "$_q_head" ] && break` + `[ "$cid" = "$_q_head" ] || continue` with `[ -n "$_q_loo_set" ] || break` + `case " $_q_loo_set " in *" $cid "*) : ;; *) continue ;; esac`
- Loop 2 (needs-analysis boards path, lines 1969-1970): same membership-check pattern applied identically.

### 2. Skip-aware eligibility set — PASS
Lines 1893-1901: iterates `$_q_order`, skipping `done|blocked|hold|deferred` (exact same case-pattern as the `_q_head` computation at lines 1859-1873), collects up to `$((_q_lookahead+1))` active entries. No positional slicing.

### 3. Write-stage guards unchanged — PASS
- CTO/approval guard (lines 1994-1997): still `[ -z "$_q_head" ] && break` / `[ "$cid" = "$_q_head" ] || continue`
- Tech-lead planning guard (lines 2221-2224): still head-only
- Plan-review convergence guard (line 2247): still `_q_plan_head`
- Conflict-resolution guard (lines 2201-2203): still head-only
- Launchable leaf guard (lines 2422-2425): still `_q_head`

### 4. N=0 serial restore — PASS
When `CB_QUEUE_LOOKAHEAD=0`: `_q_lookahead=0`. Loop collects first non-skip entry, `_q_loo_count` becomes 1, `[ 1 -ge 1 ]` triggers break → set = {head only}. Prior serial behavior restored identically.

### 5. No bypass — PASS
`git show 68522c9 | grep -i "eslint-disable\|no-verify"` returns empty. No lint or hook bypass introduced.

## Decision: APPROVED

## Acceptance (machine)

- check: gh pr list -R stratch1989/crewboss --state open --json number,reviews -q ".[].reviews" | grep -q "APPROVED"
