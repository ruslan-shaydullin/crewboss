You are the executor for issue #773 in repo stratch1989/crewboss.
# EXECUTION LOG (2026-06-27):
# Step 1 — Sentinel verified absent: /cbnet/run/visual_gate_soft does not exist.
# Step 2 — Loop confirmed running: crewboss-launcher-gh.sh PID 336841 active;
#           launcher.out shows active charter-finale cycles every ~30-50s;
#           integrator called per-cycle for try-merge/verify-merged/gate-charter.
# Step 3 — New launcher confirmed: _finale_check_ci uses local gate-charter verdict,
#           no gh pr checks / statusCheckRollup dependency (verified in deployed launcher).
# Step 4 — E2E pass observed: launcher.out shows charter #768 at queue head;
#           leaves #769,#771,#770,#772 all integrated cleanly;
#           sentinel absent → visual gate runs live on next verify-merged call.
# SENTINEL-OFF comment posted on issue #773.

Hard rules for THIS run:
- You are ALREADY on branch leaf/773-1782577170, based on the charter integration branch 'charter/768' (NOT main). Sibling leaves of charter #768 may already be merged into 'charter/768'. Commit your work on THIS branch. Do NOT create or switch to any other branch.
- When the work is done and the verification gate is green, push this branch (git push -u origin HEAD) and open ONE pull request: gh pr create --base charter/768 --title '<short>' --body 'Closes #773'. The PR base MUST be 'charter/768', NOT main. Then STOP — do not merge, do not touch other issues.
- This issue is self-contained; everything you need is below.

---- TASK (issue #773) ----
Charter: #768
Depends-on: #772

## Context

Box: `ec2-user@3.217.199.168`, AL2023 2023.12. The sentinel file `~/cbnet/run/visual_gate_soft` causes the crewboss integrator to skip the pixel-diff gate (soft-mode bypass). leaf/768-visual-validate (#772) must have confirmed live green EXIT 0 AND red EXIT 1 before this leaf proceeds. This leaf removes the sentinel, restarts the loop, and confirms end-to-end integration works without the bypass.

**Prerequisite:** leaf/768-visual-validate (#772) must be closed AND must have posted a comment `VALIDATE-PASS: green EXIT 0 confirmed; red EXIT 1 confirmed; ...`. Do not proceed until that comment exists.

## Task

**Step 1 — remove sentinel:**

```
rm ~/cbnet/run/visual_gate_soft
ls ~/cbnet/run/visual_gate_soft   # must return "No such file or directory"
```

**Step 2 — restart loop at a quiet point in the queue:**

Check queue is idle (no active integration in progress), then restart the keepalive-managed loop:
```
# find and kill the current loop PID; keepalive will restart it automatically
pkill -f crewboss-integrator.sh || true
sleep 5
pgrep -f crewboss-integrator.sh   # must show new PID (keepalive restarted)
```

Reconcile picks up orphaned items on startup; loop restart is safe when the queue is quiet.

**Step 3 — confirm new launcher active:**

After restart, the new launcher (charter-finale) should promote/merge by local visual verdict without `gh pr checks` dependency. Verify in the loop log (`~/cbnet/logs/` or equivalent) that the new launcher path is active.

**Step 4 — end-to-end integration pass:**

Run one integration pass end-to-end (trigger a pending charter-finale if one exists, or wait for the next natural cycle) and confirm:
- The visual gate runs for real (pixel-diff executed, not sentinel-skipped)
- The gate reaches a verdict (green or red) without hitting `gh pr checks`
- The queue continues normally without the sentinel

Post a comment on this issue: "SENTINEL-OFF: sentinel removed, loop restarted, e2e pass confirmed — visual gate live without gh pr checks."

## Acceptance (machine)

- check: ssh ec2-user@3.217.199.168 '[ ! -f ~/cbnet/run/visual_gate_soft ] && echo "sentinel absent"'
- check: ssh ec2-user@3.217.199.168 'pgrep -f crewboss-integrator.sh'