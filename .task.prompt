You are the executor for issue #1169 in repo stratch1989/crewboss.
Hard rules for THIS run:
- You are ALREADY on branch leaf/1169-1782909637, based on the charter integration branch 'charter/696' (NOT main). Sibling leaves of charter #696 may already be merged into 'charter/696'. Commit your work on THIS branch. Do NOT create or switch to any other branch.
- When the work is done and the verification gate is green, push this branch (git push -u origin HEAD) and open ONE pull request: gh pr create --base charter/696 --title '<short>' --body 'Closes #1169'. The PR base MUST be 'charter/696', NOT main. Then STOP — do not merge, do not touch other issues.
- This issue is self-contained; everything you need is below.

---- TASK (issue #1169) ----
Charter: #696
Depends-on: #1168

## Context

`runtime-manifest.test.sh` Test 2 was flaky due to a SIGPIPE+pipefail race. PR #996 fixed this (no early awk exit). Leaf #1168 (qa-engineer) adds a 10× determinism regression harness (`runtime-manifest-determinism.test.sh`) and registers it as `EXCLUDED` in `reference/runtime/per-leaf-manifest`, updating counts from `ALLOW=23, EXCLUDED=75, union=98` to `ALLOW=23, EXCLUDED=76, union=99`.

This leaf confirms the authoritative ALLOW-subset gate (`verify-merged.sh`) remains sound after those changes, and that all count invariants are satisfied.

## Task

After leaf #1168 has merged, verify the following:

### 1. ALLOW designation retained

Confirm that `runtime-manifest` still appears as `ALLOW` (not `EXCLUDED`) in `reference/runtime/per-leaf-manifest`. The no-early-exit awk fix in #996 strengthens the pure-function guarantee (no pipe subprocesses are abandoned mid-flight) — the ALLOW classification is correct and must not have been altered.

### 2. Counts: comment correctness

Confirm the `Counts:` comment in `reference/runtime/per-leaf-manifest` reads exactly:
```
ALLOW=23, EXCLUDED=76, union=99
```

### 3. leaf-verifier.test.sh Test 7 green

Run `reference/tests/leaf-verifier.test.sh` and confirm Test 7 passes. Test 7 dynamically counts EXCLUDED entries and actual `.test.sh` files in the tree — only `ALLOW=23` is a hardcoded literal. After leaf #1168 adds `runtime-manifest-determinism.test.sh` + its EXCLUDED entry, the dynamic count will be 76 EXCLUDED / 99 actual, satisfying the ALLOW+EXCLUDED==union==actual invariant.

### 4. verify-merged.sh + regen-manifest.sh sequencing

Confirm that the `verify-merged.sh` invocation of `regen-manifest.sh` (synchronous, no `&`, change-gated) introduces no new hazard after the fix. The gate still completes before any test runs.

### 5. Optional: wire determinism run into CI

Optionally update the `engine` CI job configuration to run `runtime-manifest-determinism.test.sh` as an additional sanity step after the standard test suite. This is a nice-to-have, not required for charter closure.

## Files expected (read-only verification, no code changes needed unless step 3/4 reveals an issue)

- `reference/runtime/per-leaf-manifest` — verify ALLOW entry present, Counts: updated
- `reference/tests/leaf-verifier.test.sh` — run Test 7, confirm green
- `reference/tests/runtime-manifest-determinism.test.sh` — confirm file exists (created by #1168)

## Acceptance (machine)

- check: grep -q '^ALLOW runtime-manifest$' reference/runtime/per-leaf-manifest
- check: grep -q 'EXCLUDED=76' reference/runtime/per-leaf-manifest
- check: grep -q 'union=99' reference/runtime/per-leaf-manifest
- test: reference/tests/leaf-verifier.test.sh
- check: bash reference/tests/runtime-manifest-determinism.test.sh